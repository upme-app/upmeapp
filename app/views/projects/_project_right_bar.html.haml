-# Modal Convide seu time

= render partial: 'project_team'

- if @project.client_id != current_user.id
  %a.btn-primary{data: {toggle: "modal", target: "#invitation-modal"}}
    Convide seu time

.modal.fade#invitation-modal
  .modal-dialog
    .modal-content
      .modal-header
        .modal-title Convide seu time
      .modal-body
        = form_tag invite_user_to_project_path(@project.id) do |f|
          = text_field_tag :email, '', class: 'form-control', placeholder: 'Digite o email de quem você quer convidar.'
      .modal-footer
        %a.btn-default{data: {dismiss: "modal"}} Cancelar
        %button.btn-primary{type: :submit} Convidar