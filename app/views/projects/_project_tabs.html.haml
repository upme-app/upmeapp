%ul.upme-tab
      %li= link_to 'Projeto', project_path(project.id), class: 'upme-tab-title'
      %li= link_to 'Linha do Tempo', timeline_path(project.id), class: 'upme-tab-title'
      %li= link_to 'Solicitações', invitations_path(project.id), class: 'upme-tab-title'